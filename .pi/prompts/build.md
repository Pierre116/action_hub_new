Build the React frontend:
```bash
cd action_hub/frontend && npm install && npm run build
```
Report any TypeScript or build errors. If the build succeeds, confirm the output is in `action_hub/static/dist/`.

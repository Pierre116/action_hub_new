Compare the spec requirements for the current NEXT batch (from `AGENTS.md`) against the existing code. For each spec requirement:
- ✅ Implemented correctly
- ❌ Missing or incorrect (describe the gap)
- ⚠️ Partially implemented (describe what's missing)

Read the spec section referenced in the batch, then read the target source files. Output a structured gap analysis.
